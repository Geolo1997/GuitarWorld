<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="pers.geolo.guitarworldserver.dao.CommentDAO">
    <insert id="add" parameterType="comment" useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO Comment(works_id, author, create_time, content)
        VALUES (#{worksId}, #{author}, #{createTime}, #{content});
    </insert>


    <delete id="remove" parameterType="int">
        DELETE
        FROM Comment
        WHERE id = #{id};
    </delete>

    <select id="getComment" parameterType="int" resultType="comment">
        SELECT id, works_id, author, create_time, content
        FROM Comment
        WHERE id = #{id};
    </select>

    <select id="listCommentOfWorks" resultType="comment">
        SELECT id, works_id, author, create_time, content
        FROM Comment
        WHERE works_id = #{worksId};
    </select>

</mapper>